### 🔹 커밋 취소 + staged 상태 유지

```bash
git reset --soft HEAD~1
```

### 🔹 커밋 취소 + unstaged 상태 (가장 많이 씀)

```bash
git reset HEAD~1
```
- 기본값: `--mixed`

#### 원격 저장소에 이미 푸시한 커밋을 취소하고 싶을 때

```bash
git reset HEAD~1              # (1) 로컬: 커밋 제거 + changed 상태
git push origin HEAD --force  # (2) 원격: 현재 로컬 상태로 강제 덮어쓰기
```

### 🔹 커밋 취소 + 파일까지 전부 삭제

```bash
git reset --hard HEAD~1
```