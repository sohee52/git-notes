# 브랜치 이름 변경

## 1️⃣ 로컬 브랜치 이름 변경

### ✅ 현재 브랜치 이름을 바꾸는 경우

```bash
git branch -m 새_브랜치명
```

### ✅ 다른 브랜치 이름을 바꾸는 경우

```bash
git branch -m 기존_브랜치명 새_브랜치명
```

---

## 2️⃣ 원격 브랜치까지 변경 (이미 push한 경우)

> **핵심:**
> 로컬 이름 변경 → 새 이름으로 push → 기존 원격 브랜치 삭제

### ① 로컬 브랜치 이름 변경

```bash
git branch -m 기존_브랜치명 새_브랜치명
```

### ② 새 이름으로 원격에 push

```bash
git push origin 새_브랜치명
```

### ③ 기존 원격 브랜치 삭제

```bash
git push origin --delete 기존_브랜치명
```

---

## 3️⃣ upstream(branch tracking) 다시 설정 (중요)

브랜치 이름을 바꾸면 upstream 연결이 끊깁니다.

```bash
git push --set-upstream origin 새_브랜치명
```

또는 짧게:

```bash
git push -u origin 새_브랜치명
```

---

## 4️⃣ 확인 방법

```bash
git branch        # 로컬 브랜치
git branch -r     # 원격 브랜치
git branch -vv    # upstream 연결 상태까지 확인
```

---

## ⚠️ 주의사항 (실무에서 중요)

* **PR이 이미 열려 있다면**

  * GitHub에서는 **브랜치 이름 변경 = 새 브랜치**로 인식
  * → PR 다시 만들어야 할 수 있음
* **다른 사람이 해당 브랜치를 사용 중이면**

  * 반드시 공지 후 변경
* `main`, `develop` 같은 보호 브랜치는
  → GitHub 설정에서 먼저 보호 해제 필요

---

## 🔥 가장 흔한 실전 시나리오

```bash
# feature/10 → feature/member-auth 로 변경
git branch -m feature/10 feature/member-auth
git push origin feature/member-auth
git push origin --delete feature/10
git push -u origin feature/member-auth
```