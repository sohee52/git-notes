**로컬 브랜치 삭제하는 방법**

| **목적** | **명령어** | **비고** |
| --- | --- | --- |
| **로컬** 브랜치 삭제 (안전) | `git branch -d <브랜치이름>` | **병합 완료**된 브랜치 삭제 (권장) |
| **로컬** 브랜치 삭제 (강제) | `git branch -D <브랜치이름>` | **주의!** 병합 여부와 상관없이 삭제 |